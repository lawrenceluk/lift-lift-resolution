# Supabase (public, safe for frontend)
VITE_SUPABASE_URL=https://xxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGc...

# Supabase (secret, backend only - never commit these!)
# Required for server-side database operations (bypasses RLS)
SUPABASE_SERVICE_ROLE_KEY=<ask in secure vault>
# SUPABASE_DB_PASSWORD=<ask in secure vault>
